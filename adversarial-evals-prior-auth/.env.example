# Copy to .env. Only needed when you run against a real model.
# Omit all of this and use --offline to run the scripted stub instead.

# Agent under test. LangChain "provider:model" identifier.
PA_MODEL=anthropic:claude-opus-5

# Model that grades the adversarial cases.
PA_JUDGE_MODEL=anthropic:claude-opus-5

ANTHROPIC_API_KEY=
